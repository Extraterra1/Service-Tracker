# TV Board Deslop Refinement Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Remove decorative dashboard treatments from `#tv` and tighten its operational hierarchy.

**Architecture:** Keep data and routing untouched. Simplify the TV component markup and its scoped CSS, protecting the change with component and style regression tests.

**Tech Stack:** React, CSS, Vitest, Vite, in-app browser viewport testing.

---

### Task 1: Specify the simplified markup

**Files:**
- Modify: `src/features/tv/__tests__/TvOperationsBoard.test.jsx`
- Modify: `src/features/tv/TvOperationsBoard.jsx`

1. Add a failing assertion that `01` and `02` are absent while both service labels remain.
2. Run the focused component test and confirm failure.
3. Remove the decorative index spans and keep semantic labels.
4. Re-run the focused component test.

### Task 2: Remove ornamental CSS and tighten the grid

**Files:**
- Modify: `src/features/tv/__tests__/tvBoardStyles.test.js`
- Modify: `src/App.css`

1. Add failing assertions that the delivery has no pseudo-element side stripe and empty-state type is restrained at the compact breakpoint.
2. Run the focused style test and confirm failure.
3. Remove the stripe and index styling, simplify the section grid, reserve red for flight time, and reduce empty-state size.
4. Re-run all focused TV tests.

### Task 3: Verify and merge

1. Inspect empty and populated states at 961×541 with no overflow.
2. Run full tests, changed-file lint, and production build.
3. Commit, merge into `master`, and verify the merged result.
