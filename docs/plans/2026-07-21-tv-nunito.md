# TV Nunito Typography Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Bundle Nunito locally and apply it only to the hidden TV workspace.

**Architecture:** Add the variable Nunito package as a build dependency, import its Latin stylesheet from the TV workspace, and update the scoped TV font stack. Leave global typography untouched.

**Tech Stack:** React, CSS, Fontsource, Vitest, Vite.

---

### Task 1: Specify TV-only Nunito loading

**Files:**
- Modify: `src/features/tv/__tests__/tvBoardStyles.test.js`
- Create: `src/features/tv/__tests__/TvWorkspace.font.test.js`

1. Add failing assertions for the Nunito TV font stack and local Fontsource import.
2. Run the focused tests and confirm failure.

### Task 2: Bundle and apply Nunito

**Files:**
- Modify: `package.json`
- Modify: `package-lock.json`
- Modify: `src/features/tv/TvWorkspace.jsx`
- Modify: `src/App.css`

1. Install `@fontsource-variable/nunito`.
2. Import the Latin variable stylesheet in `TvWorkspace.jsx`.
3. Apply Nunito only to `.tv-board`, with a rounded system fallback.
4. Run focused TV tests.

### Task 3: Verify and merge

1. Inspect computed font and zero-overflow bounds at 961×541.
2. Run full tests, changed-file lint, and build.
3. Commit and merge into `master`.
