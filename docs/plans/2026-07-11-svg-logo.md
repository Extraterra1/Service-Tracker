# SVG Logo Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Use the supplied JustDrive SVG for both application logo placements.

**Architecture:** Store the SVG alongside other bundled assets and keep the current image component API unchanged. Only the imported asset path changes, so layout and interactions remain stable.

**Tech Stack:** React, Vite asset imports, Vitest

---

### Task 1: Protect SVG usage

**Files:**
- Create: `src/components/__tests__/logoAsset.test.js`

**Step 1: Write the failing test**

Read both logo component source files and assert they import `Logo Base.svg`, with no PNG references.

**Step 2: Run the test**

Run: `npx vitest run src/components/__tests__/logoAsset.test.js`

Expected: FAIL because both components still import the PNG.

### Task 2: Replace the asset

**Files:**
- Create: `src/assets/Logo Base.svg`
- Modify: `src/components/AppHeaderMenu.jsx`
- Modify: `src/components/SignedOutLanding.jsx`
- Delete: `src/assets/Logo Just Drive Madeira-1.png`

**Step 1: Copy the supplied SVG**

Copy `/Users/cpires/JustDrive/JustDrive Logos/Logo Base.svg` into `src/assets/Logo Base.svg`.

**Step 2: Update imports and remove the PNG**

Point both components at the SVG and delete the now-unused PNG.

**Step 3: Run focused tests**

Run the logo regression and header behavior tests. Expected: PASS.

### Task 3: Verify and integrate locally

**Step 1: Run `npm test`, changed-file lint, `git diff --check`, and `npm run build:perf`.**

**Step 2: Commit on `codex/svg-logo` and fast-forward local `master`.**

Do not push or deploy.
